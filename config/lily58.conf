# --- BLE essentials ---
CONFIG_ZMK_BLE=y
CONFIG_BT=y
CONFIG_BT_PERIPHERAL=y
CONFIG_BT_MAX_PAIRED=5

# Name shown during pairing
CONFIG_BT_DEVICE_NAME="Lily58"

# 961 = HID Keyboard (do NOT inline-comment the assignment)
# Ref: Bluetooth Assigned Numbers → Appearance Values
CONFIG_BT_DEVICE_APPEARANCE=961

# Optional: avoid passkey UI on the keyboard
CONFIG_ZMK_BLE_PASSKEY_ENTRY=n
